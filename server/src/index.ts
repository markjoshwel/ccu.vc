// Server entry point - will be implemented in US-003
import type { RoomState, ClientToServerEvents, ServerToClientEvents } from '@ccu/shared';

// Type check that imports work
type _RoomStateCheck = RoomState;
type _C2SCheck = ClientToServerEvents;
type _S2CCheck = ServerToClientEvents;

console.log("CCU Server placeholder");
