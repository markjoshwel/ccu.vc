{
  "name": "server",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "tsc --outDir dist",
    "typecheck": "tsc --noEmit",
    "dev": "bun run src/index.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/bun": "latest"
  },
  "dependencies": {
    "shared": "workspace:*",
    "socket.io": "^4.8.3"
  }
}
