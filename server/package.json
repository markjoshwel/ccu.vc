{
  "name": "@ccu/server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@ccu/shared": "*",
    "nanoid": "^5.1.6",
    "socket.io": "^4.7.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "socket.io-client": "^4.8.3",
    "typescript": "^5.3.0"
  }
}
